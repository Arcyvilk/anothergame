import React from 'react';
import styled from 'styled-components';

const StyledHeader = styled.div`

`;

export default function Header() {
  return (
    <StyledHeader>ANOTHER GAME (ONLINE)</StyledHeader>
  )
}